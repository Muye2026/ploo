# Product Loop V2 说明

`product-loop` 用来编排小型硬件产品的概念视觉、工业设计、Fusion 360 建模、EasyEDA 原理图和 PCB、用户跟画以及外部交接。

Fusion 360 MCP、立创 EDA API/Skill、图片和视频工具都是可选适配器，不是安装依赖。即使一个外部工具都没有，用户仍可用 Product Loop 完成需求、架构、Design Pack、Electrical Pack、Interface Control、验收计划、跟画说明和外部交接。缺少工具只会让对应的直接执行路线暂时不可用，不得自动安装工具、自动换路线或阻断无关的规划工作。

## 核心原则

Agent 可以只读检查、整理输入、比较方案、给出推荐，并在用户已批准的路线内连续执行可逆步骤；但以下选择必须由用户作出：

- 是否生成图片或视频
- 是否建模以及由 MCP、用户还是外部人员完成
- 是否画原理图和 PCB
- EasyEDA 采用直接、跟画、混合还是交接
- 架构、关键器件、设计方向、冻结、冲突、约束放宽和高风险操作

推荐不等于授权。能力不足时不得静默降级，必须回到用户决策。

## 四份核心产物

- `design-pack.v2.json`：产品与工业设计真相。
- `electrical-pack.v2.json`：电气、原理图和 PCB 输入真相。
- `interface-control.v2.json`：外壳与电路板共用的毫米级接口真相。
- `run-state.v2.json`：路线、决策、能力、依赖、证据和运行状态。

## 工作流

先做只读能力探测，再让用户选择视觉、机械、原理图和 PCB 路线。工业设计与电气架构可以并行；PCB 需要原理图和共享接口冻结，Fusion 建模需要外观/结构和共享接口冻结。CAD 与 PCB 最后做跨域核对，再形成 EVT 验证计划。

每个直接或跟画步骤都使用 Operation Card，明确目标、对象、预期变化、不要动什么、回滚方式、验收方法和证据。直接执行卡只绑定一个经过实时探测的 provider 操作；写前先校验完整 bundle，再做只读门禁、一次性预留、单次调用和回读完结，重试必须使用新的 attempt。只有 API 回读、源文件导出或清晰截图可以支持“已实现/已验证”。

## 安装和测试

可安装 Skill 位于内层 `product-loop/`。推荐把它符号链接到 `${CODEX_HOME:-$HOME/.codex}/skills/product-loop`，以后更新仓库即可同步；复制安装的 V1 用户必须先备份旧目录，避免重复复制后产生嵌套目录。完整安装、V1→V2 更新、数据迁移和回滚说明见 [UPGRADING.md](UPGRADING.md)。运行测试：

```bash
python3 -m unittest discover -s tests -v
```

公开示例只能使用合成数据，不得写入私人项目、机器路径、真实 UUID、账号、密钥或环境配置。
